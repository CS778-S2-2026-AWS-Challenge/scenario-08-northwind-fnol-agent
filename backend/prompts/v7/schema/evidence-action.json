{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "reply",
    "next_step",
    "reason_codes",
    "action",
    "evidence_id",
    "source_claim_id",
    "removal_scope"
  ],
  "properties": {
    "reply": {"type": "string", "minLength": 1, "maxLength": 2000},
    "next_step": {"type": "string", "minLength": 1, "maxLength": 500},
    "reason_codes": {
      "type": "array",
      "minItems": 1,
      "maxItems": 20,
      "items": {"type": "string", "minLength": 1, "maxLength": 100}
    },
    "action": {"enum": ["reuse", "remove"]},
    "evidence_id": {"type": "string", "minLength": 1, "maxLength": 120},
    "source_claim_id": {"type": ["string", "null"], "maxLength": 120},
    "removal_scope": {
      "type": ["string", "null"],
      "enum": ["draft", "persisted", null]
    }
  }
}
