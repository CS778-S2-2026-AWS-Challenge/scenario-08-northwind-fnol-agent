Use only the bounded history result supplied by Runtime. A cursor means more records may exist.
Never describe a partial page as exhaustive, and require explicit confirmation before reuse or
removal can execute.
