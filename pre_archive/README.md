# Presentation Archive

This directory contains code and tests used only by the temporary single-origin presentation
environment. The archived files are retained for traceability and are not part of the formal
application runtime or API contract.

Future product work must start from `main` and must not restore these files without an explicit
scope decision.
