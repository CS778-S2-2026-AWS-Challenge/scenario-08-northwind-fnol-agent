# Candidate profile. Provider bindings are deliberately absent and startup is refused.
NORTHWIND_ENVIRONMENT=development
DATA_RUNTIME_PROFILE=cloudflare
NORTHWIND_OBJECT_STORAGE_ADAPTER=fixture
AGENT_RUNTIME_PROFILE=controlled
